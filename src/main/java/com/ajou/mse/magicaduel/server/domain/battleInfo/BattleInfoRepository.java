package com.ajou.mse.magicaduel.server.domain.battleInfo;

import org.springframework.data.jpa.repository.JpaRepository;

public interface BattleInfoRepository extends JpaRepository<BattleInfo, Long> {
}
